{
  "name": "absolute-path",
  "displayName": "Absolute Path",
  "version": "0.0.1",
  "publisher": "YoshinoriN",
  "engines": {
    "vscode": "^1.22.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./out/extension",
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "Absolute path extention configurations",
      "properties": {
        "absolutePath.defaultPathStyle": {
          "type": "string",
          "default": "unix",
          "enum": [
            "unix",
            "windows"
          ],
          "description": "Specify default path style. Unix like or Windows like.",
          "scope": "window"
        }
      }
    },
    "commands": [
      {
        "command": "absolutePath.showQuickPicker",
        "title": "Absolute Path: Open Menus"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "npm run compile && node ./node_modules/vscode/bin/test"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.42",
    "@types/node": "^7.0.64",
    "tslint": "^5.10.0",
    "typescript": "^2.6.1",
    "vsce": "^1.40.0",
    "vscode": "^1.1.17"
  }
}
