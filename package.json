{
  "name": "absolute-path",
  "displayName": "Absolute Path",
  "description": "Display absolute or from workspace path of current file in StatusBar by Unix style or Windows style.",
  "version": "1.0.0",
  "publisher": "YoshinoriN",
  "repository": {
    "type": "git",
    "url": "https://github.com/YoshinoriN/vscode-absolute-path"
  },
  "license": "MIT",
  "engines": {
    "vscode": "^1.0.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "absolute path",
    "workspace path",
    "path"
  ],
  "icon": "images/icon.png",
  "activationEvents": [
    "*"
  ],
  "main": "./out/extension",
  "contributes": {
    "configuration": {
      "type": "object",
      "title": "Absolute path extention configurations",
      "properties": {
        "absolutePath.defaultPathStyle": {
          "type": "string",
          "default": "unix",
          "enum": [
            "unix",
            "windows"
          ],
          "description": "Specify default path style. Unix like or Windows like.",
          "scope": "window"
        },
        "absolutePath.priorityInStatusBar": {
          "type": "number",
          "default": 0,
          "description": "The priority in the statusBar. Higher values shown more to the left.",
          "scope": "window"
        },
        "absolutePath.isFromWorkSpaceRoot": {
          "type": "boolean",
          "default": false,
          "description": "Shown from workspace root path or not.",
          "scope": "window"
        }
      }
    },
    "commands": [
      {
        "command": "absolutePath.executeQuickPickerAction",
        "title": "Absolute Path: Open Menus"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "npm run compile && node ./node_modules/vscode/bin/test",
    "package": "vsce package ./node_modules/.bin/vsce package"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.42",
    "@types/node": "^7.0.64",
    "tslint": "^5.10.0",
    "typescript": "^2.6.1",
    "vsce": "^1.40.0",
    "vscode": "^1.1.17"
  },
  "dependencies": {
    "clipboardy": "^1.2.3"
  }
}
